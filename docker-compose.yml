version: '3.8'

services:
  newsletter:
    container_name: MediConnect
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"  
    environment:
      - NODE_ENV=production
